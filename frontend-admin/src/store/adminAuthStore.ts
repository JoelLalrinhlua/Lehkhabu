/**
 * adminAuthStore.ts
 *
 * Admin auth state is managed directly in AdminLayout using supabase.auth.getSession()
 * and admin_accounts table checks — no separate Zustand store is needed.
 *
 * Reserved as a placeholder in case admin auth needs to be extracted in the future.
 */
